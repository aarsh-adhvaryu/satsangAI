# SatsangAI V1 backend — runtime dependencies (the api/ FastAPI app).
# Pinned to the versions the app was built + evaluated against on the Studio box.
# torch is installed separately in the Dockerfile from the CPU wheel index so the
# image doesn't pull the multi-GB CUDA build (V1 retrieval runs fine on CPU).
#
# Heavy enrichment/tuning deps (peft, datasets, the Gemma stack) are intentionally
# NOT here — they belong to the offline enrichment/eval pipelines, not the app.

fastapi==0.136.3
uvicorn[standard]==0.49.0
anthropic==0.109.2

# Retrieval: BGE-M3 query embeddings + bge-reranker-v2-m3 cross-encoder.
sentence-transformers==5.5.1
transformers==5.12.1

# Index load + vector math.
pandas==2.1.4
pyarrow==24.0.0
numpy==1.26.4
pydantic==2.13.4

# Postgres (pgvector) store backend — used only when SATSANG_STORE=postgres;
# harmless to have installed for the default in-memory store.
psycopg[binary]==3.3.4
pgvector==0.4.2
